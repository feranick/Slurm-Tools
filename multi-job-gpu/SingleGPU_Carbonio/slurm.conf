# slurm.conf for host "carbonio"
# 12 CPUs, ~40 GB RAM, 1x NVIDIA GeForce GTX 1080 Ti (11 GB, Pascal)
# GPU sharing enabled via NVIDIA MPS.
# Put this file (identical) on every node: /etc/slurm/slurm.conf
# See the slurm.conf(5) man page for details.

ControlMachine=carbonio
#ControlAddr=

MpiDefault=none
ProctrackType=proctrack/cgroup
ReturnToService=1

SlurmctldPidFile=/run/slurmctld.pid
#SlurmctldPort=6817
SlurmdPidFile=/run/slurmd.pid
#SlurmdPort=6818

SlurmdSpoolDir=/var/lib/slurm-llnl/slurmd
SlurmUser=slurm
#SlurmdUser=root
StateSaveLocation=/var/lib/slurm-llnl/slurmctld
SwitchType=switch/none

# task/cgroup gives real core/memory/GPU confinement once a node is shared.
# Requires the accompanying cgroup.conf. Use TaskPlugin=task/none for no enforcement.
TaskPlugin=task/cgroup

#
# TIMERS
#KillWait=30
#MinJobAge=300
#SlurmctldTimeout=120
#SlurmdTimeout=300

#
# SCHEDULING
SchedulerType=sched/builtin
# cons_tres is REQUIRED to subdivide the node (per-core, per-GPU, MPS).
# select/linear would give the whole node to one job.
SelectType=select/cons_tres
SelectTypeParameters=CR_Core_Memory

#
# GENERIC RESOURCES (GPU + MPS)
GresTypes=gpu,mps

#
# LOGGING AND ACCOUNTING
AccountingStorageType=accounting_storage/none
ClusterName=carbonio
JobAcctGatherType=jobacct_gather/cgroup
#SlurmctldDebug=info
SlurmctldLogFile=/var/log/slurm-llnl/slurmctld.log
#SlurmdDebug=info
SlurmdLogFile=/var/log/slurm-llnl/slurmd.log

#
# COMPUTE NODES
# 1 GPU. mps:100 = 100 "percent" of compute for the single GTX 1080 Ti.
# A job requesting --gres=mps:8 gets ~8% -> up to ~12 jobs share the card,
# matching the 12 CPUs and staying under Pascal's 16-client MPS limit.
NodeName=carbonio CPUs=12 Boards=1 SocketsPerBoard=1 CoresPerSocket=6 ThreadsPerCore=2 RealMemory=38000 Gres=gpu:1,mps:100 State=UNKNOWN

PartitionName=long Nodes=carbonio Default=YES MaxTime=INFINITE State=UP
